{% extends "@Blog/layout.html.twig" %}

{% block content %}
    <div class="col-lg-6">
        {%  for message in app.session.flashbag().get('status') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}
        <h2>Todas las etiquetas</h2>
        <hr>
        <a class="btn btn-success" href="{{ path("blog_add_tag") }}">Añadir etiqueta</a>

        <table class="table table-bordered">
            <thead>
            <tr>
                <th>Nombre</th>
                <th>Descripcion</th>
                <th>Eliminar</th>
            </tr>
            </thead>
        {%  for tag in tags %}
        <tr>
            <td>{{ tag.name }}</td>
            <td>{{ tag.name }}</td>
            <td><a class="btn btn-danger" href="{{ path("blog_delete_tag",{"id": tag.id})}}">Eliminars</a></td>
        </tr>
        {% endfor %}
        </table>
    </div>
    <div class="clearfix"></div>
{% endblock %}